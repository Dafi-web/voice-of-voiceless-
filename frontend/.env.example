# Required on Vercel — your Render backend (no trailing slash).
# Used at build time to proxy /api and /uploads; browser calls stay same-origin.
VITE_API_URL=https://voice-of-voiceless-1.onrender.com
