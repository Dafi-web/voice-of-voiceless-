ADMIN_PASSWORD=rahwa2026
JWT_SECRET=change-this-to-a-long-random-string
PORT=3001

# Optional — use MongoDB Atlas instead of local SQLite (site.db)
# Paste your connection string from MongoDB Atlas → Connect → Drivers
MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster.mongodb.net/beyond-silence?retryWrites=true&w=majority
