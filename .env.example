# Local development
ADMIN_PASSWORD=rahwa2026
JWT_SECRET=change-this-to-a-long-random-string
PORT=3001

# Render production (set in Render dashboard, not in .env file on Render)
# DATA_DIR=/opt/render/project/src/data
# UPLOADS_DIR=/opt/render/project/src/data/uploads
# NODE_ENV=production

# Vercel frontend only — point to your Render backend URL
# VITE_API_URL=https://your-app.onrender.com
